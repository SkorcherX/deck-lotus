node_modules/
.env
data/*.db
data/*.db-*
# The test environment's disposable working copy of the fixture
data/test-run/
data/*.sqlite
data/*.bz2
client/dist/
client/node_modules/
*.log
.DS_Store

# OCR and card-detection assets are staged from node_modules at build time,
# not committed — see client/scripts/copy-ocr-assets.mjs
client/public/tesseract/
client/public/opencv/

# Card hash build intermediates. The packed data/card-hashes.bin IS committed —
# it is what ships so the server never has to fetch 112k images — but the raw
# jsonl it is packed from is a ~15MB build artifact and the log is noise.
data/card-hashes.raw.jsonl
data/card-hashes.normal.jsonl
data/card-hashes.build.log

# Replay working directory: extracted captures and the Scryfall references they
# are measured against. Rebuilt from the bundles whenever a hash change is
# weighed — see docs/SCAN_DIAGNOSTICS_TESTING.md — and about 19MB of JPEGs.
out/
